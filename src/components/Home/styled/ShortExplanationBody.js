import styled from 'styled-components';

export default styled.p`
  font-size: 22px;
  line-height: 38px;
  text-align: center;
  width: 709px;
  margin: 0 auto;
  font-family: ${p => p.theme.fonts.body};
`;
